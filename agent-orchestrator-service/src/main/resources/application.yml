server:
  port: 8084

spring:
  application:
    name: agent-orchestrator-service

  # --- Redis ---
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 5000ms

  # --- Kafka ---
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: orchestrator-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.idfcfirstbank.agent.*
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

# --- LLM Provider Configuration ---
# Enable one or more providers via llm.<provider>.enabled=true
# Multiple providers can be enabled simultaneously for LlmRouter failover.
# llm.provider selects the primary; others are used as fallbacks.
llm:
  provider: ${LLM_PROVIDER:ollama}
  timeout: ${LLM_TIMEOUT:30s}
  ollama:
    enabled: ${LLM_OLLAMA_ENABLED:true}
    base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
    model: ${OLLAMA_MODEL:llama3.1}
  anthropic:
    enabled: ${LLM_ANTHROPIC_ENABLED:false}
    api-key: ${ANTHROPIC_API_KEY:}
    model: ${ANTHROPIC_MODEL:claude-sonnet-4-20250514}
  openai:
    enabled: ${LLM_OPENAI_ENABLED:false}
    api-key: ${OPENAI_API_KEY:}
    model: ${OPENAI_MODEL:gpt-4o}
    base-url: ${OPENAI_BASE_URL:https://api.openai.com}
  azure:
    enabled: ${LLM_AZURE_ENABLED:false}
    endpoint: ${AZURE_OPENAI_ENDPOINT:}
    api-key: ${AZURE_OPENAI_KEY:}
    deployment: ${AZURE_OPENAI_DEPLOYMENT:gpt-4o}
    api-version: ${AZURE_OPENAI_API_VERSION:2024-02-15-preview}

# --- Agent Platform Configuration ---
agent:
  vault:
    base-url: ${VAULT_POLICY_URL:http://vault-policy-service:8082}

  routing:
    account-service-url: ${ACCOUNT_SERVICE_URL:http://agent-account-service:8085}
    card-service-url: ${CARD_SERVICE_URL:http://agent-card-service:8088}
    loans-service-url: ${LOANS_SERVICE_URL:http://agent-loans-service:8090}

# --- Actuator & Observability ---
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    tags:
      application: ${spring.application.name}

# --- API Docs ---
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# --- Logging ---
logging:
  level:
    com.idfcfirstbank.agent.orchestrator: DEBUG
    org.springframework.statemachine: INFO
