# Local debug profile - points to Docker Desktop services + Ollama
# Usage: mvn spring-boot:run -pl agent-account-service -Dspring-boot.run.profiles=local
server:
  port: 8085

spring:
  # Exclude Anthropic so only OpenAI ChatModel exists (pointed at Ollama)
  autoconfigure:
    exclude:
      - org.springframework.ai.autoconfigure.anthropic.AnthropicAutoConfiguration
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:29092
  ai:
    openai:
      api-key: ollama
      base-url: http://localhost:11434/v1
      chat:
        options:
          model: llama3.1
          temperature: 0.1

agent:
  llm:
    provider: ollama
    model-name: llama3.1
  vault:
    base-url: http://localhost:8082
  mcp:
    core-banking-url: http://localhost:8086

logging:
  level:
    com.idfcfirstbank.agent.account: DEBUG
    org.springframework.ai: DEBUG
