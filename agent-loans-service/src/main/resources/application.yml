server:
  port: 8090

spring:
  application:
    name: agent-loans-service

  # --- Spring AI LLM Configuration ---
  ai:
    anthropic:
      api-key: ${LLM_API_KEY:}
      chat:
        options:
          model: claude-sonnet-4-20250514
          max-tokens: 4096
          temperature: 0.7

    openai:
      api-key: ${OPENAI_API_KEY:}
      chat:
        options:
          model: gpt-4o
          max-tokens: 4096
          temperature: 0.7

  # --- Redis ---
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 5000ms

  # --- Kafka ---
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: loan-agent-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.idfcfirstbank.agent.*
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

# --- Agent Platform Configuration ---
agent:
  llm:
    provider: ${LLM_PROVIDER:anthropic}
    model-name: ${LLM_MODEL:claude-sonnet-4-20250514}
    api-key: ${LLM_API_KEY:}
    temperature: 0.7
    max-tokens: 4096
    timeout: 30s

  vault:
    base-url: ${VAULT_POLICY_URL:http://vault-policy-service:8082}

  mcp:
    credit-bureau-url: ${MCP_CREDIT_BUREAU_URL:http://mcp-credit-bureau:8091}
    account-aggregator-url: ${MCP_ACCOUNT_AGGREGATOR_URL:http://mcp-account-aggregator:8092}
    loan-origination-url: ${MCP_LOAN_ORIGINATION_URL:http://mcp-loan-origination:8093}
    core-banking-url: ${MCP_CORE_BANKING_URL:http://mcp-core-banking-server:8086}

# --- Actuator & Observability ---
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    tags:
      application: ${spring.application.name}

# --- API Docs ---
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# --- Logging ---
logging:
  level:
    com.idfcfirstbank.agent.loans: DEBUG
    org.springframework.ai: INFO
